<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora | Home</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body style="margin-top: 100px">
    <div class="navbar fixed-top bg-danger-subtle">
        <div class="container d-flex justify-content-between align-items-center">
            <h1 class="text-danger">Quora</h1>
            <div>
                <a href="/posts" class="btn btn-success">New Post</a>
            </div>
        </div>
    </div>

    <div class="container ">

        <% for(post of posts) { %>
            <div class="card m-5">
                <div class="card-body">
                    <p class="card-title fw-semibold">

                        @<%= post.username %>
                    </p>
                    <p class="card-text">
                    <p class="display-6">
                        <%= post.title %>
                    </p>

                    <%= post.content %>
                        </p>
                        <a href="#" class="btn btn-primary">Edit Post</a>
                        <a href="#" class="btn btn-danger">Delete Post</a>
                </div>
            </div>
            <% } %>
    </div>
</body>

</html>